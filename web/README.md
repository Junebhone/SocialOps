# web

SocialOps dashboard (Next.js 15 App Router, Tailwind v4, shadcn/ui).

Do not run `npm run dev` directly — the app runs as the `web` service in Docker Compose.
See the root [README](../README.md) "Run it" section: `make up`, then http://localhost:3000.
